general:
  artifacts:
    - bin/hookup

dependencies:
  pre:
    - go get github.com/Masterminds/glide
    - glide up

deployment:
  master:
    branch: master
    commands:
      - go build -ldflags '-s -w' -o bin/hookup cmd/hookup.go