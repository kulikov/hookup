general:
  artifacts:
    - bin/hookup

dependencies:
  override:
    - go get github.com/Masterminds/glide
    - glide up

deployment:
  master:
    branch: master
    commands:
      - go build -v -ldflags '-s -w' -o bin/hookup cmd/hookup.go